{
  "name": "bettervibe-admin",
  "version": "0.1.0",
  "private": true,
  "description": "Operator scripts for BetterVibe. Run on your laptop with AWS + LLM creds.",
  "scripts": {
    "bootstrap-characters": "ts-node --prefer-ts-exts bootstrap-characters.ts",
    "generate-avatars": "ts-node --prefer-ts-exts generate-character-avatars.ts",
    "seed-characters": "ts-node --prefer-ts-exts seed-characters.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.0",
    "@aws-sdk/client-dynamodb": "^3.515.0",
    "@aws-sdk/client-s3": "^3.515.0",
    "@aws-sdk/lib-dynamodb": "^3.515.0",
    "openai": "^4.65.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
